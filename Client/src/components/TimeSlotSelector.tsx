
type TimeSlotSelectorProps = {
    selectedSlot: Number | null;
    setSelectedSlot: (slot:any) => void;
};

export default function TimeSlotSelector( {selectedSlot, setSelectedSlot}: TimeSlotSelectorProps) {
    return(
        <>
        </>
    )
}